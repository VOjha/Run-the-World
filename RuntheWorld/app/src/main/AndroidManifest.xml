<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.run_the_world">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".joinTripProgressActivity"></activity>
        <activity android:name=".tripProgressActivity" />
        <activity android:name=".summaryActivity" />
        <activity android:name=".InputRunData" />
        <activity android:name=".DuringRun" />
        <activity android:name=".CreateNewRun" />
        <activity android:name=".distance_log" />
        <activity android:name=".achievements" />
        <activity android:name=".socialActivity" />
        <activity android:name=".TravelBookActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>